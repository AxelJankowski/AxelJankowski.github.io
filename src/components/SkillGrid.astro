---
import { skills } from "../data/site";
---

<div class="bevel-in p-3">
  <div class="flex flex-wrap gap-3">
    {
      skills.map((skill) => (
        <div
          class="flex flex-col items-center gap-1 p-2 bevel-out min-w-[72px]"
          title={skill.name}
        >
          {"icon" in skill && skill.icon ? (
            <img
              src={skill.icon}
              alt={skill.name}
              class="h-10 w-10 object-contain"
              width="40"
              height="40"
              loading="lazy"
            />
          ) : (
            <span
              class="flex h-10 w-10 items-center justify-center bevel-in text-[10px] font-bold text-[#000080]"
            >
              {skill.name.slice(0, 2).toUpperCase()}
            </span>
          )}
          <span class="text-[10px] text-center leading-tight">{skill.name}</span>
        </div>
      ))
    }
  </div>
</div>
